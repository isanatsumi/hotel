{% extends 'base-admin.html.twig' %}

{% block title %}Edit Reserva{% endblock %}

{% block body %}
    <h1>Edit Reserva</h1>

    {{ include('reserva/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reserva_index') }}" class="btn btn-outline-secondary">back to list</a>

    {{ include('reserva/_delete_form.html.twig') }}
{% endblock %}
