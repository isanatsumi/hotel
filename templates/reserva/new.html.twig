{% extends 'base-admin.html.twig' %}

{% block title %}New Reserva{% endblock %}

{% block body %}
    <h1>Create new Reserva</h1>

    {{ include('reserva/_form.html.twig') }}

    <a href="{{ path('reserva_index') }}" class="btn btn-outline-secondary">back to list</a>
{% endblock %}
